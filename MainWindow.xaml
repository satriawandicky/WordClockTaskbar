<Window x:Class="WordClockTaskbar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WordClockTaskbar.ViewModels"
        Title="WorldClock"
        Width="420" Height="32"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="Transparent">

    <Window.DataContext>
        <vm:ClockViewModel/>
    </Window.DataContext>

    <Border x:Name="RootBorder"
            CornerRadius="4"
            Padding="8,0">
        <ItemsControl ItemsSource="{Binding Timezones}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,6,0">
                        <TextBlock Text="{Binding Label}"
                                   Foreground="{DynamicResource LabelColor}"
                                   Opacity="0.6"
                                   Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding Time}"
                                   Foreground="{DynamicResource ForegroundColor}"
                                   FontFamily="Segoe UI Variable"
                                   FontSize="12"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding GMTOffset, StringFormat=' {0}'}"
                                   Foreground="{DynamicResource LabelColor}"
                                   Opacity="0.6"
                                   Margin="2,0,0,0"/>
                        <TextBlock Text=" | "
                                   Foreground="{DynamicResource LabelColor}"
                                   Opacity="0.3"
                                   Margin="6,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</Window>
